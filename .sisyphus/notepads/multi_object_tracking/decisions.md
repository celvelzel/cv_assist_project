## 2026-03-24T00:00:00Z Task: session-init
- Use Protocol-based abstractions (`IntentParser`, `SpatialResolver`) with dependency injection.
- Support camera-facing mode via configurable mapping (`outward` vs `user`) to resolve left/right consistently.
- Introduce lightweight tracker wrapper module to isolate external tracking dependency from system logic.
