## 2026-03-24T00:00:00Z Task: session-init
- Existing system currently picks first detection (`detections[0]`), no object tracking IDs.
- ASR parsing currently rule-based and string-only; needs abstraction for future LLM parser.
- User explicitly requires architecture openness for viewpoint inversion and parser replacement.
