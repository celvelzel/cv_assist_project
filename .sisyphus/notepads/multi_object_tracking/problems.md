## 2026-03-24T00:00:00Z Task: session-init
- Unresolved: exact `norfair` API compatibility with existing bbox dict schema should be validated in tests.
- Unresolved: deciding fallback behavior when spatial modifier cannot be satisfied (e.g., "left cup" but only one ambiguous cup).
